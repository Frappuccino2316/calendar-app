import React from 'react';

const Teams: React.FC = () => {
  // const [team, setTeam] = React.useState('');

  // React.useEffect(() => {

  // }, [])

  return (
    <div>
      <h1>Teams</h1>
      <p>所属チーム</p>
      {/* <p>{teamName}</p> */}
    </div>
  );
};

export default Teams;
