import React from 'react';

const Settings: React.FC = () => {
  // React.useEffect(() => {

  // }, [])

  return (
    <div>
      <p>Settings</p>
      <span>ユーザー名</span>
      <span>メールアドレス</span>
    </div>
  );
};

export default Settings;
